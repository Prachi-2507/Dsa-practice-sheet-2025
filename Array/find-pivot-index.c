 int removeDuplicates(int* nums, int numsSize) {
       int i=0;
       int x;
     int num;
       int arr[num];
     for (int j=1; j<numsSize; j++){
        if(nums[i]==nums[j]){
            printf("i");
            i++;
            for(int k=0; k<num ;k++){
                printf("%d",arr[i]);
            }
        }
     }
     int x = arr[i];
     int elemnts = sizeof(arr)/sizeof(arr[0]);
     printf("%d", elemnts);
     }
